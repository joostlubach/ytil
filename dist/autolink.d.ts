/**
 * Finds a single link in a text and returns it.
 *
 * @param text The text to find the links in.
 * @returns The found link.
 */
export declare function autolinkHref(text: string): string | null;
